# booking-JP-system-backend

Production-ready Booking backend scaffold (Spring Boot, Java 21, PostgreSQL, Helm)

## Quickstart (local)

Prerequisites: Java 21, Maven, Docker (for Testcontainers and local DB)

1. Build:

```bash
mvn -B -DskipTests=false clean package
